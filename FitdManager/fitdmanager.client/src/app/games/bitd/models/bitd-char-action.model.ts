import { AttributeModel, TrackModel } from "@sheet/models";

export interface BitdCharActionModel {
  playbookXp: TrackModel;
  insight: AttributeModel;
  prowess: AttributeModel;
  resolve: AttributeModel;
}
